/* @flow */
import type { SpectroConfig } from '~/types/EditorTypes';

const SpectroDefaults: SpectroConfig = {
  label: null,
  void: false,
  accepts: [],
  props: {},
  textEditable: false,
  spellCheck: false,
};

export default SpectroDefaults;
