import theme from 'themes/default';

export default {
    container: {
        display: 'flex',
        borderRadius: theme.border.width * 2,
        boxShadow: '0 4px 8px rgba(0, 0, 0, 0.075)'
    }
};
